# RULE

No leaf = not PrestigeSF Inc.

1. One schema. Same nine fields. Same four `app` values.
2. Live path is ML-DSA-65 only. No hybrid ad if the mint is lattice-only.
3. SOFTWARE custody. FIPS 140-3 is NOT_CLAIMED.
4. Deadline uses the same mint. `--mint` calls prestige-mcp.
5. Verify is a public page plus `verify-chain` on the ledger file.

A wrap with `sig` null is not a leaf.
QuantumRain intercept `edr_*` is not a leaf.
DeadlineSF.net copy about receipts is not a leaf.
Only prestige-mcp mints.
